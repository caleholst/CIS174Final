@model List<Book>
@{
    ViewData["Title"] = "Books";
}

<h1> Welcome To Filler Name </h1>

<div class="mb-2">
    <a asp-controller="Book" asp-action="Add" class="btn btn-primary btn-lg btn-block">Add New Book</a>
</div>
<div class="mb-2">
    <a asp-controller="Home" asp-action="Books" class="btn btn-secondary btn-lg btn-block">View Books</a>
</div>